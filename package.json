{
  "name": "business-workbench",
  "private": true,
  "version": "1.0.0",
  "description": "Monorepo dev launcher for backend+frontend",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix frontend install",
    "postinstall": "npm run setup",
    "dev": "concurrently -k -n backend,frontend -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev -- --open\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
